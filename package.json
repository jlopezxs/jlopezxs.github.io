{
  "private": true,
  "license": "MIT",
  "scripts": {
    "devel": "harp server src",
    "compile": "harp compile src dist",
    "publish": "npm run compile && bin/publish"
  },
  "devDependencies": {
    "critical": "^0.7.1",
    "gh-pages": "^0.11.0",
    "harp": "^0.20.1"
  }
}
