{
  "private": true,
  "license": "MIT",
  "scripts": {
    "develop": "harp server src",
    "compile": "harp compile src dist",
    "publish": "npm run compile && bin/publish"
  },
  "devDependencies": {
    "gh-pages": "^0.11.0",
    "harp": "^0.20.1"
  }
}
